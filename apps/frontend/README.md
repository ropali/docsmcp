# frontend

React frontend for DocsMCP using Vite.

## Run

```bash
npm install
npm run dev
```

The dev server runs on `http://127.0.0.1:5173` and proxies `/api` to the backend on `http://127.0.0.1:8000`.

## Build

```bash
npm run build
```
